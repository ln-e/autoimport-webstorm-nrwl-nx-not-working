import { OlegDirective } from './oleg.directive';

describe('OlegDirective', () => {
  it('should create an instance', () => {
    const directive = new OlegDirective();
    expect(directive).toBeTruthy();
  });
});
